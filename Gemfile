source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in vagrant-serverspec.gemspec
gemspec
gem "vagrant-ssh-config", git: "https://github.com/reallyenglish/vagrant-ssh-config.git", branch: "master"
